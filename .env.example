# Database (SQLite) - relative path, works on any computer
DATABASE_URL="file:./db/custom.db"

# Z.AI API Key for OCR feature (get yours free at https://z.ai)
# Without this, OCR returns placeholder data (manual input still works)
ZAI_API_KEY=""
