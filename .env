# Database (SQLite) - relative path, works on any computer
DATABASE_URL="file:./db/custom.db"

# Z.AI API Key for OCR feature (get yours at https://z.ai)
# Without this, OCR will use fallback regex parsing (lower accuracy)
ZAI_API_KEY=""
